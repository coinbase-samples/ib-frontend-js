import OrderId from './orderId';

export default {
  component: OrderId,
  title: 'OrderId',
};

const Template = (args) => <OrderId {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
