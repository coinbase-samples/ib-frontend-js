import AssetId from './assetId';

export default {
  component: AssetId,
  title: 'AssetId',
};

const Template = (args) => <AssetId {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
